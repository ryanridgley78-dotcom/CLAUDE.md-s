{
  "name": "yahsmisfits-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "deploy": "wrangler pages deploy . --project-name=yahsmisfits --branch=main"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
